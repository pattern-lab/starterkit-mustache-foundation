<legend>{{ radioLegend }}</legend>
{{# radioList }}
<input type="radio" name="{{ radioName }}" value="{{ radioValue }}" id="{{ radioId }}" required><label for="{{ radioId }}">{{ radioLabel }}</label>
{{/ radioList }}